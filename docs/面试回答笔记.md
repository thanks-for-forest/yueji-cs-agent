# 面试回答笔记 — 悦己美妆智能客服 Agent（YUEJI CS Agent）

> 岗位：AI 应用开发岗求职者 ｜ 记录人：候选人 ｜ 实时更新

## 项目一句话简介

基于 **LangGraph（Supervisor-Worker）+ RAG（Dense+BM25 dense-first 融合）+ Function Calling + 三级情绪转人工** 的美妆电商智能客服系统，覆盖商品咨询、订单查询、退换货、护肤推荐四大场景，Python/FastAPI/Streamlit 全栈实现，最终评测 100 条测试集问题解决率 100%、幻觉率 0%、检索 Recall@10 98.9%。

## 版本更新（2026-08-25，5 个新 commit）

1. **feat: 用户绑定**（12ca911）：会话归属校验 + 订单按用户隔离
   - API：`POST /api/session` 绑定 user_id；chat/history 归属校验不一致返回 403；`GET /api/sessions?user_id=` 过滤
   - 工具层：注册表通过 `_ctx` 注入 user_id（**不进 LLM 工具契约**）；订单/物流/售后 `_fetch_order` 校验 `order.user_id`，他人订单视为未找到（**不泄露存在性**）
   - 前端：侧边栏用户 ID 输入，聊天请求带身份；兼容未绑定会话（user_id="" 保持原尾号校验行为）
2. **feat: 〔来源〕可点击跳转知识库原文**（e8af074）：`GET /api/source/{label}` 来源详情页（深色科技风 HTML）+ `/raw` 原始 JSON；`parse_sources` 提取干净 source_id（P011/F020/POL-1）
3. **fix: 纯产品名来源 404**（5aa85db）：`_resolve_sources` 支持 带编号标签/纯编号/纯名称 三种形态（按产品名/FAQ问题/政策类型名匹配）
4. **fix: 前端推荐卡片 NameError**（01e3718）：`_render_order_card/_render_recommendations` 移到历史渲染循环之前（模块级代码自上而下执行的坑）
5. **fix: 创建会话 422**（2586d76）：`create_session` body 可缺省

---

## 版本更新 · v2.0（2026-08-25，LangGraph + 自研可观测 + 检索优化，累计 50 项测试全过）

6. **feat: LangGraph Supervisor-Worker 编排**（90c68d9）：主路径改为 StateGraph（security→emotion→route Supervisor→5 Worker→finalize），流式路径保留；图节点/编排器接入自研追踪；`AgentResult.to_dict` 补 meta_updates；指代查询检索增强；护肤推荐品类优先
7. **feat: 检索优化 dense_first**（90c68d9）：四策略对比实验（Dense 98.9% > Hybrid-RRF 92.8% > BM25 87.2%），线上默认 dense_first 融合 + 余弦相似度门槛 0.55（相关≥0.71/无关≤0.50 实测分界），Recall@10 92.8%→98.9%
8. **feat: 确定性评测基准**（90c68d9）：订单日期锚点 + `REFERENCE_NOW`，消除日期漂移导致的不稳定
9. **feat: 自研 JSONL 追踪器**（a324eb6）：Trace/span（contextvars 并发隔离）+ 节点耗时 + LLM 计数，`scripts/trace_report.py` 产出报告/HTML 看板；Langfuse 保留可选上报（ADR-010）

---

## Q&A 记录

<!-- 每条回答追加在下方，格式：### Q{n}. 问题 / 回答 -->
